# Handoff, 2026-09-06: DT-managed native compile

For the next agent. Read `STATUS.md` for the gate table; this page is
the narrative, the evidence, the tools, and the exact open defects.
Ground rules from `ACTION_PLAN_NEXT_AGENT.md` apply verbatim: fix the
compiler at the identity, never the sources; seconds-long repro before a
six-minute lowering; one heavy run at a time; native builds and parity
only in the sequence the user authorizes; report with verification output.

## Where the session started and where it ended

Start (checkpoint f90b36ea, 2026-09-05 evening): the DT program could not
lower (`scan_managed_duplicates`: undefined operand 459), and the
return-merge repro failed (id 35 defined twice).

End (2026-09-06 morning): the DT program lowers (178 functions, zero
duplicates), emits C, compiles, runs to completion, and a native
execution tracer exists. Native parity is still red, and its root cause
is identified with evidence (below). Every fix is a separate commit with
the gate it turned green named in the message:

| Commit | What |
|---|---|
| bb3cf3d9 | control expressions consume region-owned values (return-merge repro green) |
| e1f94cc3 | record rows in tuple rows; loop-body placement (record-row repro green) |
| e4c04407 | None-specialized parameters fold; lexical effects; region-less conditionals nested (scan green) |
| ca2c1eed | record slots in ABI column order; planned callsites at authored position |
| 7a276df7 | callsite publications include projections; predicates depend on operands; None defaults carry physical dtype |
| 5921be2f | local truth queries by source position; and/or short-circuit fold of branch tests |
| 2b75777d | pruned conditional's position from its source control record |
| dbf77f32 | ordered insertion flattens nested sequences |
| 1445946f | C emitter types a callee's output slot under the callee |
| 8dc639b3 | region output projections take the region's declared dtype (native repro green) |
| 122b35e5, af238659, (this commit) | native execution tracer (`emit_ssa_to_c(trace=True)`, `--trace`, `tools/read_native_trace.py`); authored-function filter |

## What is proven (each by a tool you can run in seconds)

- `tools/repro_return_merge.py` (2 s): control-aware return merging, no
  duplicate SSA ids.
- `tools/repro_record_row_effects.py` (2 s, real Metrics/Targets ABI,
  full-native contract): the DT retry loop's shape -- a callee returning a
  constructed record, `failures.append((float(dt), metrics, tuple(reasons)))`
  inside the loop, a keyed write-back through a callee, a dead
  `attempt_log` arm -- lowers with no undefined operand and every effect
  inside the loop.
- `tools/repro_record_field_mutation_native.py` (15 s, compiles and runs
  natively): a record's array field written two calls deep changes the
  ROOT buffer, and a scalar returned from two return sites comes back
  right. This rules out record plumbing as the parity cause.
- `tools/scan_managed_duplicates.py` (5.5 min): the whole managed program
  lowers, 178 functions, zero duplicates, INOUT-REDEFINED pi_update only.
- Native builds `build/managed_dt_record_rows10_20260906` (and 9): run
  to completion, `advanced` matches eager, ten buffers mismatch.

## The open defect (native parity)

Everything the retry loop writes through the material record is
invisible at the root buffers, with one recognisable signature:
telemetry exactly 0, inputs rows 2-7 uninitialised, controller untouched,
`last_maximum_*` NaN, `dt_next` 0. Traced through the C of build 10: the
material buffers pass by pointer window -> run_superstep -> step ->
advance (`b3 -> v5 -> v480 -> v222 -> v2`), advance's regions store
through them (`impl_index_assign_double(v2, ...)`), so the writes DO
happen. What undoes them is the rollback path:

1. `saved = state.copy_shallow()` is never lowered. `saved` became a
   FABRICATED float64 scalar input: step formal 46, passed as
   run_superstep's invented input 589, up to the window. The full-native
   gate said `unresolved_calls=0` because the call vanished before the
   gate. `state.restore(saved)` IS lowered (step's `planned_region_2`,
   guarded by `rejected`, four `index_assign_double` into
   inputs/state/output/telemetry from values loaded off that scalar).
2. `targets.mass_max`, `targets.error_limits`, `ctrl.dt_min` projections
   in step are `unresolved_record_projection_receivers` (function
   metadata), so the rejection test compares against receivers the linker
   could not bind.

Traced run (build/managed_dt_trace2_20260906, `--trace` without the
function filter): the trace reached step -> advance -> vector_step and
then timed out at 300 s with a 2 GB file, because the module-lane helpers
(`binary_value` 92,276 activations, `index_select_double`) log every
element of their inner loops. The trace filter (authored functions only,
commit below) was added in response; a filtered traced build into
`build/managed_dt_trace3_20260906` was launched and NOT waited for. To
get the decisive lines: run its executable through
`tools/managed_dt_parity.py build/managed_dt_trace3_20260906 --frames 1
--timeout 300` with `TURING_TRACE_FILE` set, then
`tools/read_native_trace.py <file> --function step_with_dt --first 2000`
and `--grep 'CondBr %t428'` (the `rejected` branch), `--grep 'planned_region_2'`
(the restore), `--grep '%t46'` (the fabricated snapshot input). The two
questions those lines answer: does native reject the first attempt, and
what does restore copy.

## Tools added this session

- `TURING_TRACE_FILE=<path>` + a binary built with `--trace`
  (`tools/build_balloon_tire_native.py --managed-dt --trace ...`) writes
  one line per executed SSA instruction of every authored function
  (module-lane helpers are excluded to keep the file small): function,
  block, instruction text, value. `tools/read_native_trace.py <file>
  --entries` counts activations; `--function step_with_dt --first 400`
  prints one activation; `--grep 'CondBr %t428'` finds a branch outcome.
  Re-emitting from `<build dir>/repository-ssa.pkl` with
  `emit_ssa_to_c(module, root, entry_name=..., trace=True)` takes about a
  minute and is the fast way to check emitter changes.
- `TURING_DEBUG_LINKED_CALLS=1` prints each linked callsite's
  classification (callee outputs, result bindings, returns flags).
- `TURING_DEBUG_PHYSICAL=1` prints C type-union conflicts from the
  emitter's type solver.
- A baseline worktree at a SHORT path (`git worktree add --detach
  C:\Users\alber\AppData\Local\Temp\wtb <sha>` with `NODUS_TENSOR_CORE`
  set) is how the return-merge regression was bisected; a worktree under
  the scratchpad breaks the cffi link on path length.

## Known gaps met and left alone

- Region `less`/`greater` over loop-carried scalars have no module-lane C
  spelling (blocks while-loop variants of the native repro; the DT program
  itself compiles because its comparisons are control expressions).
- `tests/test_ir_sequence_tables.py::test_compiled_retained_loop_mutates_caller_sequence_record`
  fails identically at f90b36ea (expects an empty C shell source).
- `--trace` on the managed host traces authored functions only; the
  helpers' inner element loops would otherwise produce gigabytes.

## Recommended order for the next agent

1. Make the gate loud: a call result or record projection that becomes a
   fabricated ABI input of a linked function is a hard finding (formal 46
   / 589 would have failed the scan instead of running).
2. Lower `copy_shallow`/`restore` as what they are (a snapshot is the
   record's mutable arrays copied into caller-owned storage; restore is
   the copy back). Seconds-long repro first, in the shape of
   `tools/repro_record_field_mutation_native.py` plus a snapshot/restore
   pair, checked natively.
3. Bind the `targets`/`ctrl` projection receivers in step.
4. Rebuild, rerun `tools/managed_dt_parity.py --frames 1`, then section 5
   of the evening plan (`DESIGN_DISPATCHER_THREADS_2026-09-05.md`,
   regressions before code).

## Resumption: 2026-09-06, after d50015b4 (uncommitted)

The prior process (Windows PID18816) exited and
build/managed_dt_trace3_20260906 now contains its executable, module.c,
repository-ssa.pkl and empty shortfalls.json. No new native execution or parity
run was launched during this resumption. Build10 parity remains red.

The existing check_formal_parity diagnostic was not wired into full-native
validation. _full_native_link_failures now accepts module= and includes its
unaccounted_formals findings; lower_ast_source_to_ssa supplies the module and
reports those findings in its rejection. Invented arguments cannot pass merely
because the undefined-operand check counts every formal as a definition.
Targeted tests/test_native_formal_provenance.py::test_full_native_gate_checks_formal_provenance
passed3 in2.33s; tools/repro_return_merge.py passed (1.84s lowering, no duplicate).
Rechecking the saved trace3 SSA with the updated gate yields complete=False,
five functions with unaccounted formals: run_superstep, step (including saved46),
_propose_dt_pen, balloon_tire_managed_advance and balloon_tire_vector_step.
No full lowering was repeated. These are provenance failures, not yet proof
that every reported slot has the same cause; require a valid ABI/workspace
receipt or fix its missing producer, never blanket-account the findings away.

Additional source evidence: _normalize_record_snapshot_methods handles an
Assign whose value is directly copy_shallow(), while DT's saved assignment is
an IfExp. Restore independently rewrites using ALL mutable ProgramABI fields.
Actual BalloonTireManagedState.copy_shallow/restore copies inputs/state/output
and two scalar maxima; it deliberately excludes telemetry. Thus merely adding
IfExp support to the existing all-mutable-field rewrite would still change the
authored program. Fix by preserving authored method behavior and its identities.

Handoff projection warning needs qualification: saved trace3 metadata still
lists unresolved_record_projection_receivers, but several projections now have
program_abi_parameter/field receipts and others have no uses. Do not reject all
entries just because this historical metadata remains; inspect final bindings.
No live test/build processes remain. No commit/push. Next: a small authored
conditional snapshot/restore reproduction, then resolve its missing producer.

2026-09-06 snapshot reduction follow-up (no new production edits):
build/probe_authored_snapshot.py is a seconds-long authored class reproduction:
copy_shallow returns (self.state.copy(),), restore assigns saved[0] to state;
telemetry is mutable but excluded. Conditional saved=copy_shallow() if rollback
else None. Diagnostic monkeypatch disables ONLY the all-mutable-fields snapshot
rewriter in that process, so ordinary method ingestion is tested. Full-native
gate remains active. It rejects root formal5 (saved), with undefined_operands=0
and unresolved_calls=0. BOTH methods exist in the resulting SSA and copy_shallow
Call publishes3, but restore consumes invented5; rollback itself disappears from
root formals/control. Need inspect conditional-result identity and boolean
parameter handling; do not assume the method vanished in this reduced case.

build/probe_authored_snapshot_direct.py removes the conditional assignment and
restore guard to isolate the baseline. It lowers successfully using authored
methods, no invented root inputs. This is lowering evidence only, not native
snapshot-copy correctness. Neither reproduction changes DT source or disables
the production full-native gate. The diagnostic module pickle path
build/authored-snapshot-failed.pkl was overwritten by the direct baseline;
its name is stale, use the drivers to regenerate and inspect exact outputs.
All diagnostic subprocesses terminal. No native runs/builds started.
Next: turn the conditional-result case into a targeted regression and fix its
identity before pursuing native snapshot copy/restore parity. The full DT case
still also has the production rewriter's incompatible field selection.

### Follow-up: traced formal creation and restored call-only conditional

The conditional probe's post-reduction graph retains rollback=1 and IfExp=5
(call=3, None=4); neither is specialized. Its control program initially had
only callsite3, region0, callsite19, region1. Tracing the first request for 5
proves `emit_plan_callsite -> external_value -> arguments.append` creates the
provisional formal. It exists before call linking and has no later producer.

`_ordinary_conditional_control_programs` skipped a branch with no numerical
region or return, and its callsite receipts recognized only operation `call`.
Resolved methods instead have their method name as the operation. The builder
now preserves those branches using method/function references and exact lexical
membership, including an explicit empty else scope for else-owned calls.

New targeted regression `test_native_call_only_conditional.py::test_record_method_call_retains_runtime_conditional`
failed before the change (zero CondBr). Afterward it retains the input/branch
and compiles/runs native with false and true: state [2,3] and [7,3] respectively.
The durable regression passed in 12.17 seconds, including native execution in a
bounded child. This is a tiny native regression, not a full DT rebuild/parity.

Re-running the snapshot probe now retains rollback and guards restore; it still
fails for local saved=5 because the IfExp merge is absent. The diagnostic pickle
`build/authored-snapshot-failed.pkl` now contains this conditional failed module.
`build/snapshot-control.txt` contains the guarded-restore control program.
The original all-mutable-fields snapshot rewrite remains unchanged and wrong.
See tools/TRANSLATION_DEBUGGING.md for the producer/provisional-formal decision
path. No full DT build or parity was launched. Changes remain uncommitted.

### Follow-up: explicit conditional-expression results (still uncommitted)

`ConditionalBlock.result_aliases` now carries (true value, false value, result)
for IfExp. Unlike carried assignments it has no initial input. Conditional
planning retains the source arms; SSA captures their outgoing values and emits
a conditional_result Phi, claiming any provisional result formal. Projection
preserves this receipt and the callsite/record-projection receipts.

The native scalar regression then exposed call linking's incorrect Phi use
location: it moved both calls into the merge to dominate the Phi instruction.
The marker dominance check now uses each operand's declared incoming edge.
`test_native_conditional_call_result.py::test_conditional_call_result_has_producer`
passes in 12.69s, including native False -> 4 and True -> 8 for x=3, with exactly
the authored x/enabled root inputs. Return-merge remains OK (2.30s).

The authored snapshot diagnostic (normalizer bypassed only in memory) now
lowers and emits C with no fabricated saved input. DO NOT call it correct:
build/snapshot-authored.c shows a scalar copy, no write-back in restore, and
root mutation/read regions before snapshot capture. It was not executed native.
The saved pickle now contains this lowered conditional module.

`tools/repro_record_row_effects.py` now fails the formal provenance gate for
step value 55 (57 formals, 56 accounted). Disabling only the new IfExp control
programs in memory gives the identical failure; the new expression merge is
not responsible for this finding. Its producer still needs investigation.
No full build/parity, no commit, no live heavy process at this update.

### Follow-up: copy extent fixed; restore loss localized before SSA

The snapshot's self.state already has the exact record descriptor (2,), but
_tensor_descriptor omitted `copy` from shape-preserving operators while accepting
`clone`. AbstractTensor.copy is explicitly a clone alias. Added `copy` to the
descriptor query and structural descriptor-publication lists. The snapshot copy
now has descriptor (2,), and restore's saved parameter carries rank 1.
`tests/test_native_record_span_copy.py::test_record_span_copy_preserves_shape`
passes in 11.93s: a 2x3 record span returns all six distinct values through native
C with the correct shape. This does not prove snapshot independence or ordering.

Restore's ellipsis expansion is NOT the current loss: at the pre-SSA graph seam,
the ndim expression has correctly folded to constant (slice(None),) at value21.
IndexedStore22 is retained with base2, index21, value13. However restore's sole
dispatch region contains only Indexed13 (saved[0]); its control program contains
only that region. Store22 has no region/control representation. Trace region
classification next, rather than changing ellipsis expansion or adding a late
backend store. The diagnostic drivers are build/probe_snapshot_shapes.py and
build/probe_restore_regions.py; TURING_DEBUG_GRAPH_NODES=restore:4,6,21,22 shows
the folded index and surviving store. No full DT/native/parity run was launched.

### Follow-up: slice-store classification fixed; snapshot native still red

`_is_dispatch_metadata_node_impl` classified IndexedStore22 as coordinator
metadata because its folded index tuple contains slice objects. Basic tensor
index literals are now distinguished from non-numeric data operands. Restore's
region now owns nodes (13,22), rather than only saved[0] at node13.
`_fold_callsite_structural_values` now consumes an exact retained constant index
before decoding the original AST, so the folded ndim expression publishes
basic_index_axes and basic_index_source_shape for tensor lowering.

Harness correction: build/probe_authored_snapshot.py originally omitted
`c_backend_repository_ssa_reference()`, unlike the managed DT route. It now
supplies the real provider. The vanished store was independently proven before
that boundary; the scalar addressing diagnostics afterward were partly a
harness artifact. An exploratory fused-program change was removed because
this path uses the hierarchy's region SSA instead.

`test_native_record_span_restore.py::test_record_span_restore_writes_full_slice`
passes in 10.28s with the provider: six values restored into a 2x3 record span,
source unchanged. The bounded combined snapshot native diagnostic then ran:
False -> state [7,3], telemetry [11,20]; True -> state [7,garbage], telemetry
[11,20], expected state [2,3]. C still declares scalar t5 for the conditional
snapshot and passes &t5 to restore; restore indexes saved[0] as a scalar rather
than the authored tuple's copied array. Capture also remains after mutation.
These aggregate/ordering defects are open. Saved C: build/snapshot-authored.c;
driver: build/probe_snapshot_native.py, input SSA: build/authored-snapshot-failed.pkl.
Do not repeat native execution until representation/order is fixed. No full
DT build or parity was launched; no process remains live from this diagnostic.

### Follow-up: singleton return container loss isolated

`build/probe_singleton_snapshot.py` removes the conditional and record wrapper:
capture(array) returns (array.copy(),); recover(saved) returns saved[0]; tick
passes capture's result to recover. With DT's tensor provider and array shape
(2,3), linked SSA returns shape (3,). Recover's specialization is given a (2,3)
tensor formal and emits index_select_double for row zero. This proves tuple
loss before the conditional merge; repairing only Phi shape would not suffice.

The reducer splits tuple/list return expressions into physical output slots and
collapses one slot to its value. Added return_container_kinds, keyed by the same
exact source span as return_slot_values, recording tuple/list/value BEFORE that
collapse. `test_return_container_provenance.py::test_single_return_slot_retains_authored_container`
passes three cases in 3.14s. This is provenance preservation only: no descriptor
propagation/linking behavior has yet been changed to consume the new receipt.

Next: use the receipt to distinguish return array from return (array,) in
_propagate_callsite_tensor_specializations / call_result_descriptor, preserve
member identities when that result becomes a callee argument, then extend through
the optional conditional. Inspect _build_shell_hierarchy_plan's one-output
special case (child_output_paths == ((0,),)); it currently binds to the call
itself without distinguishing the authored container. Existing aggregate-member
ABI machinery is _apply_callsite_aggregate_descriptors and
_authored_aggregate_leaves. Do not solve this by changing snapshot source.
No native execution was needed for this localization; no heavy process is live.

## Follow-up: exact parameter provenance and singleton return structure (September 6)

The fabrication has more than one cause. Structural recovery in
`fortran_c_shell.ensure_structural_value` accepted every graph Input as a
function formal, including `binding_kind="loop"`. The history regression exposed
`step` (id 3) this way. Recovery now admits only parameter-bound inputs; a
missing local definition records `missing-local-producer:<binding_kind>` instead
of extending the public signature. This does not invent a replacement producer.

Aggregate parameter expansion is legitimate. Lowered authored functions now
retain `authored_parameters` and exact `parameter_member_formals` receipts
(parameter, index path, value id). The formal gate accepts those receipts only
when rooted in an authored parameter. Generic `source_parameter_view_root`
labels and local variable names do not suffice. Explicit empty authored
signatures are audited too; older products still use their parameter-name ledger.
Seven focused gate cases pass (1.87 s): real fields, workspace and tuple members
are accepted; invented values, local-member/local-view labels and an invented
input to a zero-argument function are rejected.

The one-element tuple return distinction is now consumed by tensor callsite
propagation. Flat authored tuple/list call results publish exact member
projections; the hierarchy binds those members instead of collapsing a singleton
container into its payload. Projection aliasing now retargets roots and both
return ledgers, preventing dead-node cleanup from deleting a returned member.
The structural fold avoids repeatedly aliasing an already-canonical member to
itself. Scope: ordinary resolved function calls and flat descriptor lists;
method/optional aggregate propagation still needs work.

Scheduled-call marker replacement now preserves the exact linked producer's
physical type while retaining the marker object used by downstream consumers.
Loop result ports also retain the normal incoming value's shape/device, not just
its dtype. The existing nested return regression initially failed the new
provenance gate (two history members plus the loop local), then exposed a scalar
loop-exit result. After these fixes it passes (4.13 s), with explicit assertions
that all formals are declared parameters or exact history members.
`tools/repro_return_merge.py` passes (1.75 s).

`test_singleton_call_result_preserves_tensor_member_natively` compiles and runs
`capture(array) -> (array.copy(),) -> recover(saved) -> saved[0]` with the real
repository tensor provider and full-native contract. All six distinct 2x3 values
and the result shape survive; there is only one authored root input. Initial
native run passed in 10.70 s. This is not snapshot rollback or full DT parity.

Latest authored snapshot diagnostic (real methods, production snapshot rewrite
bypassed in memory only) still lowers. Its entry block still runs mutation/read
regions before the conditional capture. The merged value now has shape (2,),
but its arms are a tensor and None, and restore still takes an indexed tensor
rather than the authored tuple member. Do not execute this as a correctness or
performance result. No new full DT build, frame parity run, commit or push.

## Follow-up: planner anchors for call-only conditionals (September 6)

The snapshot ordering defect was localized before SSA. Both ordinary conditional
controls had empty region sets and `anchor_region=None`; overlay therefore
appended capture and restore after the mutation and read regions. The existing
anchor mechanism was filled only by a later shell reconstruction path, which
need not run for controls already represented by the planner.

`_ordinary_conditional_control_programs` now publishes the existing anchor
before the first retained numerical region whose source positions follow the
complete conditional expression/statement. It also freezes the retained-region
iterable before repeated traversal. No new scheduling abstraction or backend
instruction was introduced.

`test_conditional_capture_precedes_later_mutation_natively` passes (10.60 s),
using the actual repository tensor provider and full-native contract. With fresh
state [2,3], the true branch captures 2 before the mutation to [7,3]; the false
branch returns -1 and still applies the mutation. The compiled artifact has
only the two authored inputs. This isolates ordering from the optional tuple
representation defect.

The authored snapshot SSA probe now has: entry branch; capture in if_true;
mutation region in if_merge; restore in if_true.1; final read and return in
if_merge.1. Its previous entry-block mutations are gone. Optional tuple/None
merging, method return container propagation and the incorrect all-mutable-field
snapshot source rewrite remain open. No native snapshot execution or full DT
build/parity was run in this follow-up. Next: retain the method's actual return
structure through the conditional merge and restore argument specialization.

## Follow-up: method return containers (September 6)

The descriptor fixed point inspected only `callee_ref`, excluding ordinary
resolved `method_ref` calls. It also declined to infer any result without a
tensor/aggregate positional argument, even when the method's record ABI already
provided the exact field types and shapes. Both gaps are corrected in
`_propagate_callsite_tensor_specializations`: resolve either reference, and allow
inference from declared `parameter_record_abi`.

`tests/test_native_method_return_container.py::test_record_method_tuple_result_preserves_member_natively`
passes (10.18 s). The actual compiler ingests Material.capture returning
`(self.state.copy(),)`, then Material.recover returning `saved[0]`. It preserves
all six distinct 2x3 values through compiled native execution with one physical
record-field input. This test uses the production pipeline, full-native contract
and repository tensor provider without monkeypatching.

The authored optional snapshot probe now correctly exposes the next failure:
full-native rejects root formal id 3, the capture call's aggregate identity.
Previously that call's missing container descriptor let the scalar IfExp merge
silently bind the payload as a tensor. It now publishes an exact member instead;
the IfExp still consumes the original aggregate handle, so the gate catches it.
The saved diagnostic pickle was refreshed before the gate rejection. It is not a
runnable/parity result. This supersedes the preceding 'snapshot still lowers'
status; the earlier capture/restore ordering fix remains in the worktree.

Remaining semantic requirement: represent a conditional aggregate's presence
and member values explicitly, preserving Python None distinctions. Do not fix
this by labeling id 3 as an external parameter, dropping the false arm, treating
None as a numeric array, or restoring every mutable record field. Method return
containers now work outside the optional merge; the production snapshot rewrite
and optional merge still need repair before DT parity can be claimed. No full
DT build, parity run, commit or push was performed.

## Follow-up: native array Phi storage (September 6)

Before implementing tuple-or-None merging, inspection found the C backend lowered
every Phi as a scalar: its declaration used the element type, its edge copy used
`scalar_operand`, and its address was `&tN`. This is independently incorrect for
an array payload, even after frontend shape preservation.

The new Python-to-native conditional tensor test first failed because its result
shape was (). Conditional-result lowering now preserves matching shape/device
when both incoming values have the same dtype and shape. With shape corrected,
the native test reproduced data corruption: choosing the right 2x3 array returned
only -7, followed by five zeros, instead of all six authored values.

C array Phis now select incoming span addresses with typed pointer locals.
Their addressed storage retains the element type separately from the pointer
local's declaration; scalar Phis retain their existing scalar convention. This
is a backend fix, not a host copy or Python callback.

Verification: initial direct-return native test passed (9.71 s). The finalized
parameterized test uses the actual repository tensor provider and both runtime
branch outcomes; its scale=True case, which feeds the selected span into a
compiled multiplication, passed in 10.30 s. Existing scalar conditional-call
result regression passed in 9.92 s. The final scale=False case passed in 10.32 s.
All cases use distinct values in every array slot and bounded native subprocesses.

This does not yet represent optional tuples. The latest authored snapshot still
fails the formal provenance gate at the container id, as recorded above. Next
work remains presence/member representation for the optional merge and removal
of the semantically incorrect all-mutable-field snapshot rewrite. No full DT
build, parity, performance run, commit or push.

## Follow-up: explicit optional-merge boundary (September 6)

A source audit rules out a shortcut based on matching guards: DT creates
`saved = state.copy_shallow() if rollback else None` but calls restore under
`if rejected`, not directly under `if rollback`. Presence therefore must travel
with the optional value; it cannot be inferred merely from the immediate call's
branch predicate. No source changes were made.

The existing SSA aggregate calling convention describes call output slots; it
is not a dynamic tagged optional representation. Likewise, the new C span Phi
selects array storage but supplies no None tag. A mixed None/payload Phi must not
be allowed through as an ordinary numeric or array merge.

Added `ssa_self_check.check_optional_merges`, included in run_all and the
full-native boundary gate as `unrepresented_optional_merges`. It identifies
None from its semantic dtype or exact NoneValue producer, and rejects mixed
None/payload Phi operands with a presence/payload diagnostic. All-None merges
remain admissible; no metadata escape hatch was added. The final native error
now prints optional merge findings alongside unaccounted formals.

Verification: four focused SSA gate cases pass (2.26 s): all None admitted,
scalar/span/unknown payload mixed with None rejected. A separate actual Python
compile of `return value if enabled else None` under the full-native contract
fails at this explicit representation boundary as intended (test passes in
2.81 s). This is a diagnostic safeguard, NOT implementation of optional support.

The optional lowering still needs a presence value and per-member payload
representation, None tests routed to presence, payload consumption under a
proven/checked present state, and native ABI publication if an optional escapes
its local scope. C array Phi support is available for present spans, but absence
must not be fabricated as a numeric span. The original snapshot normalization
also still rewrites all mutable fields and remains incorrect. No full build,
parity run, performance measurement, commit or push.

## Follow-up: remove the snapshot source rewrite (September 6)

With ordinary method tuple returns working, removed
`_normalize_record_snapshot_methods` and both its top-level and dependency AST
normalizer registrations. Its all-mutable-fields policy was not the authored
snapshot semantics: record mutability describes storage permissions, not which
fields a particular method chooses to capture or restore. No replacement
snapshot-specific AST rewrite was introduced.

New production-path regression:
`tests/test_native_authored_snapshot.py::test_native_snapshot_restores_only_authored_fields`.
Material.copy_shallow returns only `(self.state.copy(),)`; restore writes that
state span. The root captures, changes state, increments telemetry, restores,
and returns a state element. Both fields are mutable in ProgramABI. Before the
removal, native execution failed with state [7,3] rather than [2,3] (10.82 s).
After removal it passed (11.05 s). The final version executes the exact same
source eagerly in a separate fixture and compares both state fields and the
return value against native execution: passed in 10.51 s. Native state restores
to [2,3], telemetry remains [11,20], and return is 2. No compiler monkeypatch or
Python callback is used for native execution.

Updated the local optional-snapshot probe to remove its obsolete monkeypatch.
It now exercises production source ingestion unchanged. A bounded check confirms
its remaining frontier: root formal 3 is unaccounted, and Phi 5 mixes payload 3
with NoneValue 4. No unresolved calls, undefined operands or non-native
boundaries are reported. The saved module was refreshed for diagnostics, not
executed as a successful result.

Additional source clarification: `if not rollback` in dt_controller includes an
early-return lane. Different immediate guard names alone do not preclude a
proper control-flow proof that restore sees a present payload. Such a proof
must include those exits and actual SSA identities; it is not established by
name matching. The optional representation work remains open. Direct authored
snapshot/restore is now native/eager verified for this fixture. No full DT build,
frame parity, performance run, commit or push.

## Follow-up: control-owned tuple member merges (September 6)

Implemented `_publish_conditional_tuple_members` in descriptor propagation.
For flat tuples of equal arity with matching known member descriptors in both
IfExp arms, it publishes fresh member identities bound to corresponding arm
members and the predicate. The original aggregate retains those members through
real graph edges, so graph cleanup cannot remove them. The members are marked
`conditional_result_of`, excluded from numerical dispatch ownership, and lowered
through the existing ConditionalBlock result_aliases as individual Phis. Calls
consume the merged tuple's exact member ledger, never a fabricated scalar handle.
This is ordinary tuple support, not yet optional tuple support.

New native/eager regression:
`tests/test_native_conditional_tuple_result.py::test_conditional_tuple_members_reach_native_consumer`.
Both variants select an array-plus-scalar tuple, pass it to a compiled consumer,
and multiply its members. The call_arms=True variant obtains each tuple from a
real capture call; False uses literal tuples with scalar index projections.
Both test both runtime branches and all six array elements against the same
source executed eagerly, with only three authored inputs. Final True passed
10.46 s; False passed 10.96 s.

The literal case initially exposed a padded helper view (1,1,1) replacing the
scalar member's authored shape (). Shell control metadata now includes the
explicit member descriptor, and conditional-result lowering infers a matching
arm shape only when no owning metadata exists. With that fixed, both the scalar
and span Phis have their semantic shape and native execution passes. Existing
plain conditional tensor direct-return regression remains green (10.43 s).

Current scope deliberately does not infer structure for unknown descriptors,
different tuple layouts, nested tuples, or an absent arm. Those still require
appropriate representation rather than guessed correspondence. Next: use this
member merge structure with explicit optional presence and inactive payload
handling; retain the optional-merge rejection until that representation is
complete. Direct authored snapshot remains repaired as above. No full DT build,
parity/performance run, commit or push.

## Follow-up: guarded local optional tuples (September 6)

Extended the control-owned tuple merge to tuple-or-None when every direct
consumer is proven inside the matching presence guard. The presence descriptor
records the exact predicate identity, its required truth value, guarded consumer
ids, inactive payload ids and merged member ids. Logical Not is normalized by
following its exact operand, not a variable name. Presence is retained as the
existing runtime predicate plus polarity; it is not guessed from numeric data.
The receipt is also retained in Function.metadata.source_optional_values.

Present members merge with inactive values: null pointers for spans and inactive
zero cells for scalars. These cells are NOT Python None or a valid payload. The
planner rejects consumers without the proven presence guard and rejects optional
escape without a presence ABI before exposing tuple members to call binding.
C span Phi edges now distinguish a pointer-valued incoming operand from the
address of a scalar pointer cell, so null remains null rather than &null_cell.
The existing mixed-None/Phi gate remains active for unsupported representations.

Production optional snapshot probe now lowers with only rollback and the state/
telemetry fields as inputs. Its old build/check_snapshot_frontier.py wrapper
expects rejection and is obsolete; use build/probe_authored_snapshot.py directly.
The persisted module was refreshed. No snapshot-specific rewrite or monkeypatch
is used.

Verification:
- Native/eager guarded snapshot in the true arm passed 10.63 s (then named
  optional=True; final test id is [then]). Both runtime flag values are exercised,
  comparing state, telemetry and return against the exact same Python source.
- The inverted form, None in the true arm and capture in the else arm, with a
  matching `if not rollback` consumer, passed 10.98 s ([else]). Both flag values
  are exercised. The absent case never executes restore.
- Three negative source-lowering cases pass (4.08 s): unguarded consumer,
  consumer under the absent arm, and a changed predicate with the same variable
  name are rejected as lacking a proven presence guard.

This is guarded LOCAL optional tuple support, not a general optional ABI. None
identity tests, optional escape, differently shaped/nested tuples, and presence
proofs spanning early exits remain unfinished. The real DT controller requires
following its `if not rollback` early-return path to prove presence at the later
`if rejected` restore. Do not weaken the guard requirement to admit that case;
implement the source/CFG proof. No full DT build, parity/performance run, commit
or push. The complete validator goal remains open.
# Continuation: early-return presence, call ordering, and null pointers

The early-return snapshot case now compiles and executes natively with parity
against the exact authored Python source for both rollback outcomes. Targeted
node: `tests/test_native_authored_snapshot.py::test_native_snapshot_restores_only_authored_fields[early_return]`
(10.79 s, including native build/run). All snapshot modes now also assert that
inactive span constants retain explicit ptr dtype. The unsafe optional consumer
node still rejects unguarded, absent-arm, and changed-predicate uses (3.95 s).

`_source_fallthrough_predicate_facts` in glsl_deployment_strategy follows exact
graph predicate identities through authored AST paths. Return/raise/break/continue
terminate paths; joins intersect facts. Loop and exception constructs do not
export newly inferred facts. General optional escape ABI remains unsupported.

The first native early-return run restored state correctly but returned 7 instead
of 2. `build/probe_early_schedule.py` proved source Control IR put restore before
the final read, then `_schedule_loop_callsites` reversed them using the flat
hierarchy rank. Source-placed call markers are now excluded from hierarchy rank
constraints in the stable dependency sort, retaining authored order.

A separate physical type defect appeared: `_materialize_control_constants`
produced a ptr zero, but `ir_indexing._propagate_scalar_dtypes` changed it to int64
based on the Python literal. `build/probe_optional_constant.py` traces that exact
assignment. Pointer constants now preserve declared dtype; C emission explicitly
materializes null pointer constants rather than scalar storage. Unsupported
non-null pointer literals report a C shortfall.

No full DT build, parity, performance run, commit, or push was performed for this
continuation. Next: exercise the actual DT early-exit/retry shape in a bounded
lowering diagnostic before claiming the real controller is fixed. Continue to
distinguish declared ABI formals from missing local producers.

## Continuation: full formal frontier 29 -> 25 (September 6 evening)

The full-native provenance gate is now the active compile gate. The prior
definedness check treated every formal as a definition, so an internally missing
value could silently become a caller input. `build/diagnose_full_formals.py`
runs the complete managed lowering, saves the failed repository SSA and resolved
ProcessGraph in `build/full_formal_diagnostic`, and prints the authoritative
count. Baseline at the start of this continuation was exactly 29 unaccounted
formals across six functions.

Two approaches were explicitly rejected rather than retained:

- A second source-level BoolOp control family intended to guard
  `_no_exchange_observed` created competing overlays in the real step CFG. After
  adjustments it produced 32 unaccounted formals and still left the keyed load
  unresolved. The entire experiment and its test were removed. The guarded load
  must attach to the already compiled membership control at late SSA placement.
- Entry-hoisting scalar `item()` values reduced the count to 25, but inspection
  proved it read initial `dt_tensor`/`total` values rather than their current loop
  values. That version was removed before proceeding. Do not restore it.

Accepted general compiler changes:

1. `_recover_late_source_unary_operations` in `fortran_c_shell.py` recovers a
   pure `all`/`any` reduction only when its exact tensor producer is in entry and
   dominates every consumer. It reuses the provisional formal object as the
   instruction result and removes it from the ABI. In the DT compile,
   `balloon_tire_managed_advance` value 132 is now `all [130] -> 132` immediately
   after the `(8,4,14)` finite tensor producer. Formal audit: 29 -> 28.
2. `topological_reducer.py` now includes values carried exclusively by terminal
   `continue` edges. Conditional fallthrough still correctly excludes a terminal
   arm. The reducer uses its existing exact `loop_control_site_bindings` receipt
   only when every continue site agrees on one updated identity; differing
   values remain unsupported pending an explicit latch merge. This restores the
   retry loop's real `dt_tensor` and `retries` recurrence.
3. Late scalar `item()` recovery now requires the source operand's exact
   `loop_carried` Phi (`initial_value_id` equality), verifies that Phi's block
   dominates every consumer, and inserts the scalar operation after the header
   Phis. It never hoists a loop-local read from the initial value. Final saved
   SSA proves `run_superstep` value 83 consumes current-total Phi 370, while step
   values 169 and 273 consume current-dt Phi 711. None is a function formal.

The last authoritative run printed `FORMAL-DIAGNOSTIC 25`. The remaining exact
findings from `build/full_formal_diagnostic/formals.json` are:

- `run_superstep` (7): 284 boundary sequence construction, 174 `callable(hint)`,
  57 `hint()`, 215 missing `dt_cap` carried initial, 240 keyed `.get`, 124
  `first.unresolved_report`, 126 slice `[1:]`.
- `step_with_dt_control_used` (15): 557 boolean flag `or`; 514/515 record-field
  effect identities; 420 `len(floor_reasons)`; 419 `tuple(reasons)`; 167 current
  `dt_tensor.item() * 0.5`; formatted sequence values 442/450/468/476/248/207;
  attempt-log mapping rows 486/516; boolean chain 406.
- `_no_exchange_observed` (1): guarded keyed load 11.
- `_propose_dt_pen` (1): starred generator reduction 49.
- `coerce_metrics` (1): dictionary-comprehension row 0.

`balloon_tire_managed_advance` is no longer in the findings. The final focused
gate was 13 passed (`test_native_late_source_reduction.py`, formal provenance,
and the relevant reducer tests), after the loop-aware item change. The last full
audit preceded only a metadata-key rename; that rename does not affect lowering.
`git diff --check` has only the checkout's usual CRLF warnings. No full native
build, parity run, performance run, commit, or push was performed. No heavy
process remains live.

Recommended next order:

1. Recover step value 167 as a header-local Mul over current-dt Phi 711 and an
   exact 0.5 Const. Generalize through a dominance-checked pure-expression
   closure rather than naming this value.
2. Apply that closure to boolean values 557 and 406 when all operands are exact
   resident booleans; retain authored short-circuit control for expressions with
   effects or unsafe loads.
3. Repair `dt_cap` value 215 and record-field values 514/515 at their actual Phi
   incoming edges. Never satisfy these with an entry producer.
4. Add native sequence/aggregate producers for the remaining tuple, slice,
   comprehension, generator, mapping-row, and formatted-text values.
5. Attach keyed load 11 to the existing membership branch. Do not recreate a
   parallel source control hierarchy.
6. Repeat the full audit until zero; only then run native build, frame parity,
   and equivalent optimized performance measurement.

All changes remain uncommitted in the already-dirty worktree. Preserve the prior
session's edits and inspect `git diff`; do not reset or stash the tree.

## Latest continuation pointer: full-native formal frontier 24 -> effective 23

The thread continued after the 25-formal note. A dominance-checked pure scalar
expression closure reconstructed step's current-loop `item() * 0.5`, and the
next full driver run saved `FORMAL-DIAGNOSTIC 24`. A strict incoming-edge repair
for a nested conditional Phi feeding a loop initializer then removed
run_superstep's fabricated `dt_cap` formal in the saved real module, giving an
effective 23. That 23 is an in-memory application of the tested post-link pass;
it still needs a fresh full driver run before it is called authoritative.

The current investigation is record-field state versioning for step's
`metrics.error_channels` and `metrics.hard_failure` assignments. Do not prune
their apparently dead effect Phis until the actual field mutations are proven
to flow into the returned Metrics record. The concise resumption document with
exact evidence, tests, remaining categories, and next commands is
`CONTINUATION_2026-09-06_FULL_NATIVE_FRONTIER.md`.

The continuation now includes a second-opinion review brief. A reviewer should
audit the saved evidence before editing, challenge the completeness of the 23
finding count, trace the two field writes through physical record return
storage, and propose the smallest general set of compiler repairs.

## Final continuation correction: authoritative 23 and handoff boundary

A fresh complete formal driver run produced `FORMAL-DIAGNOSTIC 23` with zero
undefined operands. The nested `dt_cap` value is now correct at graph ingestion
and ordinary control lowering through exact reducer-authored Phi parents. The
late post-link Phi repair was removed along with its obsolete fixture.

The current record-field branch-state code is explicitly experimental. It is a
general attempt to version object fields across source conditionals, but only
the graph-level Phi and bounded step lowering are proven. It must not be called
landed until a native/eager regression returns the whole mutated record and
proves that the selected field version reaches its physical return layout in
both branch outcomes. The current exact frontier, known red record tests, and
next diagnostic are maintained in
`CONTINUATION_2026-09-06_FULL_NATIVE_FRONTIER.md`.
